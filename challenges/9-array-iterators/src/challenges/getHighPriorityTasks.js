/**
 * Get only the high-priority tasks.
 *
 * Goal: practice `.filter()`.
 *
 * @param {Array<{ priority: string }>} tasks
 *   The full task list.
 * @returns {Array<{ priority: string }>}
 *   A new array containing only tasks where `task.priority === 'high'`.
 *
 * Notes:
 * - Do not mutate the input array.
 * - Return an empty array if there are no high-priority tasks.
 */
export function getHighPriorityTasks(tasks) {
  // Your code here...
}
