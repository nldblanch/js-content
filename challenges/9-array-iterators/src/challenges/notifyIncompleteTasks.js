/**
 * Log a reminder for each incomplete task.
 *
 * Goal: practice `.forEach()` and side effects.
 *
 * @param {Array<{ title: string, completed: boolean }>} tasks
 *   The task list.
 * @returns {undefined}
 *   Nothing (side-effect function).
 *
 * Requirements:
 * - Only log reminders for tasks where `completed === false`.
 * - Use `console.log(...)`.
 *
 * Suggested message format:
 *   Reminder: Complete '<title>'
 */
export function notifyIncompleteTasks(tasks) {
  // Your code here...
}
