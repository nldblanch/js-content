/**
 * Check whether every task is completed.
 *
 * Goal: practice `.every()`.
 *
 * @param {Array<{ completed: boolean }>} tasks
 *   The task list.
 * @returns {boolean}
 *   `true` if every task has `completed === true`.
 *
 * Edge case:
 * - For an empty array, `.every()` returns `true`.
 */
export function areAllTasksComplete(tasks) {
  // Your code here...
}
