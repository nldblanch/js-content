/**
 * Find a single task by its `id`.
 *
 * Goal: practice `.find()`.
 *
 * @param {Array<{ id: number }>} tasks
 *   The task list.
 * @param {number} id
 *   The id to search for.
 * @returns {{ id: number } | undefined}
 *   The first matching task, or `undefined` if no task has that id.
 *
 * Requirements:
 * - Do not mutate the input array.
 * - Return `undefined` when not found (that’s what `.find()` does).
 */
export function findTaskById(tasks, id) {
  // Your code here...
}
