/**
 * Convert each task into a human-friendly string for display.
 *
 * Goal: practice `.map()`.
 *
 * @param {Array<{ title: string, priority: string }>} tasks
 *   The task list.
 * @returns {string[]}
 *   A new array of strings, one per task.
 *
 * Suggested format:
 *   "Task: <title> (Priority: <priority>)"
 *
 * Requirements:
 * - Do not mutate the input array.
 * - The returned array should be the same length as `tasks`.
 */
export function formatTasksForDisplay(tasks) {
  // Your code here...
}
