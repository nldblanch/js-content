/**
 * Check whether at least one task is high priority.
 *
 * Goal: practice `.some()`.
 *
 * @param {Array<{ priority: string }>} tasks
 *   The task list.
 * @returns {boolean}
 *   `true` if at least one task has `priority === 'high'`, otherwise `false`.
 *
 * Tip:
 * - `.some()` stops early as soon as it finds a match.
 */
export function hasHighPriorityTasks(tasks) {
  // Your code here...
}
